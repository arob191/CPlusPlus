#include <iostream>
#include <cmath>

int main()
{
    // Order of operations for arithmetic operatos:
    // Parenthesis
    // multiplication & division
    // addition & subtraction

    // int students = 23;
    // int remainder = students % 2;

    double x = 3.14;
    double y = 4;
    double z;
    double w;

    // Set variable to the max or min of two values
    // z = std::max(x,y);
    // w = std::min(x,y);
    
    // Other math functions
    // z = pow(2, 3);
    // z = sqrt(9);
    // z = abs(-3);
    // z = round(x);
    // z = ceil(x);
    // z = floor(x);

    std::cout << z << '\n';

    // // Add one
    // students++;

    // // Subtract one
    // students -=2;
    // // Or use
    // students--;

    // // Multiplication
    // students*=2;

    // // Division
    // students /= 2;

    // std::cout << students << '\n';
    // std::cout << remainder << '\n';

    return 0;
}